struct Math
{
    i32 mut value

    fn add = (i32 x) -> i32
    {
        return value + x
    }

    fn add = (i32 y) -> i32
    {
        return value + y
    }
}

Math m{5}
i32 result{m.add(10)}
return result
